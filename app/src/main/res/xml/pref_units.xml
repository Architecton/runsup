<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <ListPreference
        android:defaultValue="-1"
        android:entries="@array/pref_list_dist_units"
        android:entryValues="@array/pref_list_dist_units_values"
        android:key="unit"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_title_set_dist_units" />

</PreferenceScreen>
